server:
  port: 80

build.name: abc-tech-api
build.version: "1.0"


spring:
  datasource:
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
    username: sa
    password: sa
    driver-class-name: org.h2.Driver